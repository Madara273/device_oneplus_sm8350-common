<?xml version="1.0" encoding="utf-8"?>
<!--
    Row layout for a single band entry in the RecyclerView.
    Contains:
      - CheckBox (band label + selectable)
      - TextView for frequency hint (right-aligned)
      - "ACTIVE" badge: a small pill shown only when modem is currently on this band
        (visibility toggled in BandAdapter.onBindViewHolder via entry.isActive)
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="10dp"
    android:minHeight="52dp">

    <!-- Checkbox: band label, tap to toggle -->
    <CheckBox
        android:id="@+id/band_checkbox"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:textSize="15sp"
        android:textStyle="bold"
        android:paddingStart="4dp" />

    <!-- "ACTIVE" pill badge: shown when modem is live on this band -->
    <TextView
        android:id="@+id/band_active_badge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="ACTIVE"
        android:textSize="9sp"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:background="@drawable/badge_bg"
        android:paddingHorizontal="6dp"
        android:paddingVertical="2dp"
        android:layout_marginEnd="8dp"
        android:visibility="gone" />

    <!-- Frequency hint: right-aligned grey secondary text -->
    <TextView
        android:id="@+id/band_freq_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="?android:attr/textColorSecondary"
        android:gravity="end" />

</LinearLayout>
